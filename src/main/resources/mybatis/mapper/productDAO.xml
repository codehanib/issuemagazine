<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.issuemagazine.dao.IproductDAO">

	<select id="productList" resultType="com.springboot.issuemagazine.dto.productDTO">
		select * from product order by p_no asc
	</select>
	<select id="product_detailList" resultType="com.springboot.issuemagazine.dto.product_detailDTO">
				select
			        pd.pd_no,
			        pd.pd_content,
			        pd.p_no,
			        pdi.pdi_no,
			        pdi.pdi_image,
			        pdi.pdi_order
			     from product_detail pd
			      LEFT JOIN product_detail_image pdi
			        ON pd.pd_no = pdi.pd_no
			     WHERE pd.p_no = #{p_no}
			     ORDER BY pdi.pdi_order ASC
	</select>
	<delete id="productDelete">
		delete from product where p_no=#{p_no}
	</delete>
</mapper>