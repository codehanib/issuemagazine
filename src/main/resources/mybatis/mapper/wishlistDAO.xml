<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper 
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.issuemagazine.dao.wishlistDAO">
	<insert id="wishlistInsert">
    	insert into wishlist(w_no, m_no, p_no)
    	values(wishlist_seq.NEXTVAL, #{m_no}, #{p_no})
	</insert>
    <select id="wishlistList" parameterType="int" resultType="com.springboot.issuemagazine.dto.wishlistDTO">
    	    select w.w_no, w.m_no, w.p_no, p.p_name, p.p_price2, p.p_image
    		from wishlist w
    		join product p
        		on w.p_no = p.p_no
    		where w.m_no = #{m_no}
    		order by w.w_no desc
	</select>
	<delete id="wishlistDelete">
		delete from wishlist where w_no=#{w_no}
	</delete>
</mapper>